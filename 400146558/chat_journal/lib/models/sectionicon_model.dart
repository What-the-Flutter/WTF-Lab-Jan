import 'package:flutter/cupertino.dart';

class SectionIcon {
  final IconData icon;
  bool isSelected;
  String title;

  SectionIcon({required this.isSelected, required this.icon, required this.title});
}