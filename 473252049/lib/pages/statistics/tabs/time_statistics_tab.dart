import 'package:flutter/material.dart';

class TimeStatisticsTab extends StatelessWidget {
  const TimeStatisticsTab({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Tab(
      text: 'By time',
    );
  }
}
